<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/stylesheets/admin.css' />
    <title><%= title %></title>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11" defer></script>
    <script src="/javascripts/admin.js" defer></script>
</head>
<body>

    <button>Generate Random ID number</button>

    <table>
        <thead>
            <tr>
                <th>№</th>
                <th>Email</th>
                <th>Specific Number</th>
            </tr>
        </thead>
        <tbody>
            <% persons.forEach((person, index) => { %>
                <tr>
                    <td><%= index + 1 %></td>
                    <td><%= person.email %></td>
                    <td><%= person.specific_number %></td>
                </tr>
            <% }); %>
        </tbody>
    </table>
    
</body>
</html>